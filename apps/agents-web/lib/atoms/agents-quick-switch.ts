import { atom } from "jotai"

// Controls visibility of the quick-switch dialog
export const agentsQuickSwitchOpenAtom = atom(false)

// Tracks the currently selected chat index in the quick-switch dialog
export const agentsQuickSwitchSelectedIndexAtom = atom(0)


